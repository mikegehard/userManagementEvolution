spring:
  application:
    name: email
  cloud:
    config:
      uri: http://userManagementConfigServer.192.168.11.11.xip.io
      failFast: true
      # Pin the configuration for this commit to a specific
      # commit in the config repo. This allows v10 to be
      # unaffected by configuration changes for future
      # evolutions
      label: 9d823f33b1a6a4131a371fb68ddb7912cbdddf5f
